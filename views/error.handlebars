<div class="error-container">
  <div class="error-card">
    <div class="error-icon">
      <i class="bi bi-exclamation-triangle"></i>
    </div>
    <h1 class="error-title">{{status}} - {{message}}</h1>
    <p class="error-description">{{description}}</p>
    <div class="error-actions">
      <a href="/" class="btn btn-gold">Volver al inicio</a>
      <a href="javascript:history.back()" class="btn btn-outline-gold ">Volver atrás</a>
    </div>
    {{#if stack}}
    <div class="error-stack">
      <h3>Detalles del error:</h3>
      <pre>{{stack}}</pre>
    </div>
    {{/if}}
  </div>
</div>

<style>
  .error-container {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
    padding: 20px;
    background-color: var(--black);
    color: var(--white);
  }
  
  .error-card {
    background-color: var(--black-light);
    border: 1px solid var(--gold-border);
    border-radius: 10px;
    padding: 40px;
    max-width: 800px;
    width: 100%;
    text-align: center;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
  }
  
  .error-icon {
    font-size: 4rem;
    color: var(--gold);
    margin-bottom: 20px;
  }
  
  .error-title {
    color: var(--gold);
    font-weight: 300;
    margin-bottom: 20px;
  }
  
  .error-description {
    color: var(--white-dim);
    margin-bottom: 30px;
    font-size: 1.2rem;
  }
  
  .error-actions {
    display: flex;
    justify-content: center;
    gap: 15px;
    margin-bottom: 30px;
  }
  
  .error-stack {
    text-align: left;
    background-color: rgba(0, 0, 0, 0.3);
    padding: 15px;
    border-radius: 5px;
    margin-top: 20px;
    overflow-x: auto;
  }
  
  .error-stack h3 {
    color: var(--gold);
    margin-bottom: 10px;
    font-size: 1.1rem;
  }
  
  .error-stack pre {
    color: var(--white-dimmer);
    font-size: 0.9rem;
    white-space: pre-wrap;
  }
  
  @media (max-width: 576px) {
    .error-card {
      padding: 20px;
    }
    
    .error-actions {
      flex-direction: column;
    }
  }
</style>