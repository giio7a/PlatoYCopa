<footer class="footer">
    <div class="container">
        <div class="row">
            <!-- Logo y descripción -->
            <div class="col-lg-3 col-md-6 mb-4 footer-column">
                <a href="/" class="footer-logo-link">
                    <img src="/img/Plato_y_copa_logo.jpg" alt="Plato y Copa Logo" class="footer-logo">
                </a>
                <p class="footer-description">Servicio premium de meseros para todo tipo de eventos. Hacemos de tu ocasión un momento inolvidable.</p>
            </div>

            <!-- Tipos de eventos -->
            <div class="col-lg-3 col-md-6 mb-4 footer-column">
                <h5 class="footer-title">Eventos que Atendemos</h5>
                <ul class="footer-events">
                    {{#each tiposEventos}}
                    <li>
                        <a href="/galeria?tipo={{id}}" class="footer-event-link">
                            <i class="bi {{#if icono}}{{icono}}{{else}}bi-calendar-event{{/if}}"></i>
                            {{nombre}}
                        </a>
                    </li>
                    {{else}}
                    <li><a href="/galeria?tipo=1" class="footer-event-link"><i class="bi bi-heart"></i> Bodas</a></li>
                    <li><a href="/galeria?tipo=2" class="footer-event-link"><i class="bi bi-briefcase"></i> Eventos Corporativos</a></li>
                    <li><a href="/galeria?tipo=3" class="footer-event-link"><i class="bi bi-gift"></i> Cumpleaños</a></li>
                    <li><a href="/galeria?tipo=4" class="footer-event-link"><i class="bi bi-mortarboard"></i> Graduaciones</a></li>
                    <li><a href="/galeria" class="footer-event-link"><i class="bi bi-calendar-event"></i> Ver todos</a></li>
                    {{/each}}
                </ul>
            </div>

            <!-- Contacto -->
            <div class="col-lg-3 col-md-6 mb-4 footer-column">
                <h5 class="footer-title">Contáctanos</h5>
                <div class="footer-contact">
                    <i class="bi bi-telephone"></i>
                    <a href="tel:+522223780903" class="footer-contact-link">+52 (222) 378-0903</a>
                </div>
                <div class="footer-contact">
                    <i class="bi bi-envelope"></i>
                    <a href="mailto:platoycopa.oficial@gmail.com" class="footer-contact-link">platoycopa.oficial@gmail.com</a>
                </div>
                <div class="footer-contact">
                    <i class="bi bi-geo-alt"></i>
                    <a href="https://www.google.com/maps/search/?api=1&query=Puebla,+Puebla" target="_blank" rel="noopener noreferrer" class="footer-contact-link">Puebla, Puebla</a>
                </div>
            </div>

            <!-- QR -->
            <div class="col-lg-3 col-md-6 mb-4 footer-column">
                <h5 class="footer-title">Escanea y Reserva</h5>
                <div class="qr-box">
                    <img src="/img/qr.png" alt="Código QR para reservas" class="qr-image">
                    <p class="qr-text">Escanea para contactarnos por WhatsApp</p>
                </div>
            </div>
        </div>

        <!-- Redes sociales -->
        <div class="footer-social">
            <a href="https://www.facebook.com/share/15qpCyANjq/" target="_blank" rel="noopener noreferrer" aria-label="Facebook" class="footer-social-icon">
                <i class="bi bi-facebook"></i>
            </a>
            <a href="https://www.instagram.com/platoycopa.oficial?igsh=aHpyNWxhZGxyOWJ0" target="_blank" rel="noopener noreferrer" aria-label="Instagram" class="footer-social-icon">
                <i class="bi bi-instagram"></i>
            </a>
            <a href="https://wa.me/5212223780903" target="_blank" rel="noopener noreferrer" aria-label="WhatsApp" class="footer-social-icon">
                <i class="bi bi-whatsapp"></i>
            </a>
        </div>

        <!-- Enlaces rápidos -->
        <div class="footer-links">
            <a href="/servicios" class="footer-link">Servicios</a>
            <a href="/resenas" class="footer-link">Reseñas</a>
            <a href="/galeria" class="footer-link">Galería</a>
            <a href="/sobre-nosotros" class="footer-link">Sobre Nosotros</a>
            <a href="/contacto" class="footer-link">Contacto</a>
        </div>

        <!-- Copyright -->
        <div class="footer-bottom">
            <p class="footer-copy">&copy; 2024 Plato y Copa. Todos los derechos reservados.</p>
        </div>
    </div>
</footer>

<style>
/* Estilos para el footer */
.footer {
    background-color: var(--black);
    color: var(--white-dim);
    padding: 60px 0 30px;
    position: relative;
    border-top: 1px solid var(--gold-border);
}

.footer::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 5px;
    background: linear-gradient(to right, var(--gold), var(--gold-dark));
    opacity: 0.5;
}

.footer-column {
    margin-bottom: 30px;
}

.footer-logo-link {
    display: inline-block;
    margin-bottom: 15px;
    transition: all 0.3s ease;
}

.footer-logo-link:hover {
    transform: scale(1.05);
}

.footer-logo {
    height: 70px;
    border-radius: 8px;
    border: 1px solid var(--gold-border);
    transition: all 0.3s ease;
}

.footer-logo:hover {
    box-shadow: 0 0 15px rgba(229, 199, 107, 0.4);
}

.footer-description {
    font-size: 0.95rem;
    line-height: 1.6;
    color: var(--white-dim);
    margin-top: 15px;
}

.footer-title {
    color: var(--gold);
    font-size: 1.2rem;
    margin-bottom: 20px;
    font-weight: 500;
    position: relative;
    padding-bottom: 10px;
}

.footer-title::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    width: 40px;
    height: 2px;
    background-color: var(--gold);
}

.footer-events {
    list-style: none;
    padding: 0;
    margin: 0;
}

.footer-events li {
    margin-bottom: 12px;
}

.footer-event-link {
    color: var(--white-dim);
    text-decoration: none;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    gap: 8px;
}

.footer-event-link i {
    color: var(--gold);
    transition: all 0.3s ease;
}

.footer-event-link:hover {
    color: var(--gold);
    transform: translateX(5px);
}

.footer-event-link:hover i {
    transform: scale(1.2);
}

.footer-contact {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    margin-bottom: 15px;
}

.footer-contact i {
    color: var(--gold);
    font-size: 1.1rem;
    margin-top: 3px;
}

.footer-contact-link {
    color: var(--white-dim);
    text-decoration: none;
    transition: all 0.3s ease;
    word-break: break-word;
}

.footer-contact-link:hover {
    color: var(--gold);
    text-decoration: underline;
}

.qr-box {
    background: linear-gradient(145deg, rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.4));
    border: 1px solid var(--gold-border);
    border-radius: var(--border-radius);
    padding: 15px;
    display: flex;
    flex-direction: column;
    align-items: center;
    transition: all 0.3s ease;
}

.qr-box:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
    border-color: var(--gold);
}

.qr-image {
    width: 120px;
    height: 120px;
    margin-bottom: 10px;
    border-radius: 8px;
    border: 1px solid var(--gold-border);
}

.qr-text {
    font-size: 0.85rem;
    color: var(--white-dim);
    text-align: center;
    margin: 0;
}

.footer-social {
    display: flex;
    justify-content: center;
    gap: 15px;
    margin: 30px 0;
}

.footer-social-icon {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: linear-gradient(145deg, rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.4));
    border: 1px solid var(--gold-border);
    color: var(--gold);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.2rem;
    transition: all 0.3s ease;
}

.footer-social-icon:hover {
    background: linear-gradient(to right, var(--gold), var(--gold-dark));
    color: var(--black);
    transform: translateY(-5px) rotate(5deg);
    box-shadow: 0 5px 15px rgba(229, 199, 107, 0.4);
}

.footer-links {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 20px;
    margin-bottom: 30px;
}

.footer-link {
    color: var(--white-dim);
    text-decoration: none;
    font-size: 0.9rem;
    transition: all 0.3s ease;
    position: relative;
}

.footer-link::after {
    content: "";
    position: absolute;
    bottom: -3px;
    left: 0;
    width: 0;
    height: 1px;
    background-color: var(--gold);
    transition: width 0.3s ease;
}

.footer-link:hover {
    color: var(--gold);
}

.footer-link:hover::after {
    width: 100%;
}

.footer-bottom {
    text-align: center;
    padding-top: 20px;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
}

.footer-copy {
    font-size: 0.85rem;
    color: var(--white-dim);
    margin: 0;
}

/* Responsive */
@media (max-width: 991.98px) {
    .footer {
        padding: 40px 0 20px;
    }
    
    .footer-logo {
        height: 60px;
    }
    
    .footer-title {
        font-size: 1.1rem;
    }
    
    .qr-image {
        width: 100px;
        height: 100px;
    }
}

@media (max-width: 767.98px) {
    .footer-column {
        text-align: center;
    }
    
    .footer-title::after {
        left: 50%;
        transform: translateX(-50%);
    }
    
    .footer-contact {
        justify-content: center;
    }
    
    .footer-events li {
        display: flex;
        justify-content: center;
    }
    
    .footer-event-link:hover {
        transform: translateY(-3px);
    }
}

@media (max-width: 575.98px) {
    .footer {
        padding: 30px 0 15px;
    }
    
    .footer-logo {
        height: 50px;
    }
    
    .footer-links {
        flex-direction: column;
        align-items: center;
        gap: 15px;
    }
}
</style>

