<div class="logout-overlay">
  <div class="logout-content">
    <div class="logout-animation">
      <img src="/img/Plato_y_copa_Logo.jpg" alt="Cerrando sesión" width="100px" height="100px" class="logout-logo">
    
    <div class="logout-message">
      <h2>Cerrando sesión</h2>
      <p>Gracias por utilizar nuestros servicios</p>
      
      <div class="progress-container">
        <div class="progress-bar"></div>
      </div>
      
      <div class="redirect-message">
        Redirigiendo a la página de inicio de sesión...
      </div>
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Iniciar la barra de progreso
    const progressBar = document.querySelector('.progress-bar');
    progressBar.style.width = '0%';
    
    // Animar la barra de progreso
    setTimeout(() => {
      progressBar.style.width = '100%';
    }, 100);
    
    // Redirigir después de la animación
    setTimeout(() => {
      window.location.href = '/auth';
    }, 3000);
  });
</script>

<style>
:root {
  --gold: #E5C76B;
  --gold-light: rgba(229, 199, 107, 0.7);
  --gold-lighter: rgba(229, 199, 107, 0.3);
  --gold-dark: #c4a84d;
  --black: #000000;
  --black-light: #121212;
  --black-lighter: #1e1e1e;
  --white: #FFFFFF;
  --white-dim: rgba(255, 255, 255, 0.8);
  --white-dimmer: rgba(255, 255, 255, 0.5);
  --animation-duration: 2.5s;
}

/* Overlay principal */
.logout-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.95);
  backdrop-filter: blur(8px);
  z-index: 9999;
  display: flex;
  justify-content: center;
  align-items: center;
  animation: fadeIn 0.5s ease-out forwards;
}

.logout-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  max-width: 90%;
  width: 100%;
  max-height: 90vh;
}

/* Animación principal */
.logout-animation {
  position: relative;
  width: 300px;
  height: 300px;
  margin-bottom: 2rem;
}

.animation-container {
  position: relative;
  width: 100%;
  height: 100%;
  transform-style: preserve-3d;
  perspective: 1000px;
}

/* Plato y utensilios */
.plate-container {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) translateZ(0);
  animation: floatPlate var(--animation-duration) ease-in-out infinite;
}

.plate {
  width: 120px;
  height: 20px;
  background: linear-gradient(to bottom, var(--white), #f0f0f0);
  border-radius: 50%;
  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
  position: relative;
  transform: perspective(500px) rotateX(60deg);
}

.plate-shine {
  position: absolute;
  top: 0;
  left: 20%;
  width: 60%;
  height: 50%;
  background: linear-gradient(90deg, 
    rgba(255,255,255,0.1) 0%, 
    rgba(255,255,255,0.5) 50%, 
    rgba(255,255,255,0.1) 100%);
  border-radius: 50%;
  opacity: 0.7;
}

.utensils {
  position: absolute;
  top: -15px;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  justify-content: center;
  gap: 10px;
}

.fork, .knife {
  position: relative;
  height: 40px;
  width: 4px;
  background: linear-gradient(to bottom, 
    rgba(192,192,192,1) 0%, 
    rgba(220,220,220,1) 50%, 
    rgba(192,192,192,1) 100%);
  border-radius: 1px;
  transform-origin: bottom center;
}

.fork {
  transform: rotate(-15deg);
}

.fork:before {
  content: '';
  position: absolute;
  top: 0;
  left: -4px;
  width: 12px;
  height: 10px;
  background: linear-gradient(to bottom, 
    rgba(192,192,192,1) 0%, 
    rgba(220,220,220,1) 50%, 
    rgba(192,192,192,1) 100%);
  clip-path: polygon(0 0, 100% 0, 80% 100%, 20% 100%);
}

.knife {
  transform: rotate(15deg);
  width: 6px;
}

.knife:before {
  content: '';
  position: absolute;
  top: 0;
  left: -2px;
  width: 10px;
  height: 15px;
  background: linear-gradient(to bottom, 
    rgba(192,192,192,1) 0%, 
    rgba(220,220,220,1) 50%, 
    rgba(192,192,192,1) 100%);
  clip-path: polygon(20% 0, 80% 0, 100% 100%, 0 100%);
}

/* Copa de vino */
.glass-container {
  position: absolute;
  top: 40%;
  right: 25%;
  transform: translateY(-50%);
  animation: floatGlass var(--animation-duration) ease-in-out infinite;
  animation-delay: 0.3s;
}

.glass-bowl {
  width: 60px;
  height: 70px;
  background: linear-gradient(135deg, 
    rgba(255,255,255,0.1) 0%, 
    rgba(255,255,255,0.2) 50%, 
    rgba(255,255,255,0.1) 100%);
  border: 1px solid rgba(255,255,255,0.3);
  border-radius: 50% 50% 0 0 / 80% 80% 0 0;
  position: relative;
  overflow: hidden;
  transform-style: preserve-3d;
}

.wine-level {
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 70%;
  background: linear-gradient(to right, 
    rgba(229, 199, 107, 0.7) 0%, 
    var(--gold) 50%, 
    rgba(229, 199, 107, 0.7) 100%);
  border-radius: 0 0 30px 30px / 0 0 20px 20px;
  overflow: hidden;
}

.wine-wave {
  position: absolute;
  top: 0;
  left: 0;
  width: 200%;
  height: 10px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(255,255,255,0.4), 
    transparent);
  animation: waveAnimation 2s linear infinite;
  transform: translateX(-50%);
}

.glass-stem {
  width: 6px;
  height: 40px;
  background: linear-gradient(to right, 
    rgba(255,255,255,0.2), 
    rgba(255,255,255,0.5), 
    rgba(255,255,255,0.2));
  margin: 0 auto;
}

.glass-base {
  width: 30px;
  height: 5px;
  background: linear-gradient(to right, 
    rgba(255,255,255,0.2), 
    rgba(255,255,255,0.5), 
    rgba(255,255,255,0.2));
  border-radius: 50%;
  margin: 0 auto;
}

.logout-logo{
  display: block;
  margin: 0 auto;
  text-align: center;

}

/* Mano de mesero */
.hand {
  position: absolute;
  bottom: 30%;
  left: 30%;
  width: 40px;
  height: 60px;
  transform: rotate(20deg);
  animation: serveHand var(--animation-duration) ease-in-out infinite;
}

.glove {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 70%;
  background-color: var(--white);
  border-radius: 10px 10px 0 0;
}

.cuff {
  position: absolute;
  bottom: 0;
  left: -5px;
  width: 110%;
  height: 30%;
  background-color: var(--black-lighter);
  border-top: 2px solid var(--gold);
}

/* Mensaje de cierre de sesión */
.logout-message {
  text-align: center;
  margin-top: 1rem;
  opacity: 0;
  animation: fadeInMessage 0.5s ease-out 0.5s forwards;
}

.logout-message h2 {
  color: var(--gold);
  font-size: 2rem;
  margin-bottom: 0.5rem;
  font-weight: 300;
  letter-spacing: 1px;
}

.logout-message p {
  color: var(--white-dim);
  font-size: 1.1rem;
  margin-bottom: 1.5rem;
}

/* Barra de progreso */
.progress-container {
  width: 100%;
  max-width: 300px;
  height: 4px;
  background-color: var(--black-lighter);
  border-radius: 2px;
  overflow: hidden;
  margin: 1.5rem auto;
}

.progress-bar {
  height: 100%;
  background: linear-gradient(90deg, var(--gold-dark), var(--gold), var(--gold-light));
  width: 0%;
  transition: width 3s cubic-bezier(0.1, 0.5, 0.2, 1);
  border-radius: 2px;
  box-shadow: 0 0 10px rgba(229, 199, 107, 0.5);
}

.redirect-message {
  color: var(--white-dimmer);
  font-size: 0.9rem;
  margin-top: 1rem;
  font-style: italic;
}

/* Animaciones */
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes fadeInMessage {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

@keyframes floatPlate {
  0%, 100% { transform: translate(-50%, -50%) translateZ(0) rotate(0deg); }
  50% { transform: translate(-50%, -60%) translateZ(10px) rotate(2deg); }
}

@keyframes floatGlass {
  0%, 100% { transform: translateY(-50%) rotate(0deg); }
  50% { transform: translateY(-60%) rotate(-2deg); }
}

@keyframes serveHand {
  0%, 100% { transform: rotate(20deg) translateY(0); }
  50% { transform: rotate(15deg) translateY(-10px); }
}

@keyframes waveAnimation {
  0% { transform: translateX(-50%); }
  100% { transform: translateX(0%); }
}

/* Estilos responsivos */
@media (min-width: 1200px) {
  .logout-animation {
    width: 400px;
    height: 400px;
  }
  
  .plate {
    width: 150px;
  }
  
  .glass-bowl {
    width: 70px;
    height: 80px;
  }
}

@media (max-width: 768px) {
  .logout-animation {
    width: 250px;
    height: 250px;
  }
  
  .plate {
    width: 100px;
  }
  
  .glass-bowl {
    width: 50px;
    height: 60px;
  }
  
  .logout-message h2 {
    font-size: 1.8rem;
  }
  
  .logout-message p {
    font-size: 1rem;
  }
}

@media (max-width: 480px) {
  .logout-animation {
    width: 200px;
    height: 200px;
  }
  
  .plate {
    width: 80px;
    height: 15px;
  }
  
  .utensils {
    top: -12px;
  }
  
  .fork, .knife {
    height: 30px;
  }
  
  .glass-bowl {
    width: 40px;
    height: 50px;
  }
  
  .glass-stem {
    height: 30px;
  }
  
  .hand {
    width: 30px;
    height: 45px;
  }
  
  .logout-message h2 {
    font-size: 1.5rem;
  }
  
  .logout-message p {
    font-size: 0.9rem;
  }
  
  .progress-container {
    max-width: 250px;
  }
}

/* Ajustes para dispositivos en modo landscape */
@media (max-height: 500px) and (orientation: landscape) {
  .logout-content {
    flex-direction: row;
    align-items: center;
    justify-content: center;
    gap: 2rem;
  }
  
  .logout-animation {
    width: 180px;
    height: 180px;
    margin-bottom: 0;
  }
  
  .logout-message {
    text-align: left;
    margin-top: 0;
  }
  
  .progress-container {
    margin: 1rem 0;
  }
}

/* Ajustes para pantallas muy pequeñas */
@media (max-height: 400px) {
  .logout-animation {
    width: 150px;
    height: 150px;
  }
  
  .plate {
    width: 70px;
    height: 12px;
  }
  
  .glass-bowl {
    width: 35px;
    height: 45px;
  }
  
  .logout-message h2 {
    font-size: 1.3rem;
    margin-bottom: 0.3rem;
  }
  
  .logout-message p {
    font-size: 0.8rem;
    margin-bottom: 1rem;
  }
  
  .progress-container {
    margin: 0.8rem auto;
  }
}

/* Mejoras de accesibilidad */
@media (prefers-reduced-motion: reduce) {
  .plate-container,
  .glass-container,
  .hand,
  .wine-wave {
    animation: none;
  }
  
  .progress-bar {
    transition: width 3s linear;
  }
}
</style>
